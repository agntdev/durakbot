# Telegram bot token (from BotFather)
BOT_TOKEN=

# Redis (backend storage — set one of REDIS_URL or SUPABASE_URL+SUPABASE_KEY)
REDIS_URL=

# Supabase (alternative backend storage — table schema below)
# Tables must be created by the database owner before the bot runs:
#   CREATE TABLE durak_store (key TEXT PRIMARY KEY, value TEXT NOT NULL);
#   CREATE TABLE durak_sets (set_key TEXT NOT NULL, member TEXT NOT NULL, PRIMARY KEY (set_key, member));
SUPABASE_URL=
SUPABASE_KEY=
